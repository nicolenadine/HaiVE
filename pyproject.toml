[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "haive"
version = "0.1.0"
description = "Multi-provider AI agent harness for software development"
requires-python = ">=3.11"
dependencies = [
    "litellm",
    "pydantic>=2.0",
    "pydantic-settings",
    "typer",
    "pytest",
    "opentelemetry-sdk",
    "openinference-instrumentation-litellm",
    "arize-phoenix",
    "PyGithub",
    "filelock",
    "pyyaml",
]

[project.scripts]
haive = "haive.cli:app"

[tool.hatch.build.targets.wheel]
packages = ["haive"]

[tool.pytest.ini_options]
testpaths = ["tests"]
